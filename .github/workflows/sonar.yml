name: SonarQube Scan

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  sonarQube:
    runs-on: self-hosted  # 👈 Esto ejecuta el job en tu máquina local
    steps:
      - uses: actions/checkout@v3

      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v1
        env:
          SONAR_HOST_URL: http://localhost:9000
          SONAR_TOKEN: sqp_e64608ae14e48f41212422e2bc303c0f00c0ca33
        with:
          args: >
            -Dsonar.projectKey=Desarrollo-Web
            -Dsonar.sources=.

